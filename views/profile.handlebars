{{!-- ! This file contains html for the profile page --}}

<div class="profile-main">
  <div class="row">
    <div class="col-auto">
      <h2>Welcome, {{name}}! </h2>
    </div>
  </div>
  <div class="new-post">
    <a href="/newPost"><button class="newPost-btn">New Post!</button></a>
  </div>

  <div class="profile-posts">
  {{#if blogPost.length}}
  {{!-- {{log blogPost.length}} --}}
  <div class="col-md-6">
    <h3 class="your-posts">Your Posts:</h3>

    {{#each blogPost as |blogPost|}}
    <div class="post-container">
      <div class="col-md-8">
        <h4 class="profile-post-name"><a href="/blogPost/{{blogPost.id}}">{{blogPost.name}}</a></h4>
        {{!-- <p class="created-by">Created by: {{user.name}} </p> --}}
        <p class="created-by">Created on: {{date_created}}</p>
      </div>
      <div class="col-md-4">
        <button class="delete-btn" data-id="{{blogPost.id}}">Delete</button>
      </div>
    </div> 
    {{/each}}
  </div>
  {{/if}}
</div>

{{!-- {{#if blogPost.length}}
  {{#each blogPost as |blogPost|}}
      <div class="card col-11 m-4 p-1">
        <div class="card-title d-flex justify-content-between p-2 h4">
            <a href="/blogPost/{{id}}">
            <h2 class="profile-post-name">{{blogPost.name}}</h2>
            </a>
        </div>
        <div class="card-body">{{blogPost.description}}</div>
    </div>
    {{/each}}
{{else}}
    <div class="card col-11 m-4 p-1">
        <div class="card-title d-flex justify-content-between p-2 h4">
            <p class="h2 text-dark">There are no posts</p>
        </div>
        <div class="card-body"></div>
    </div>
{{/if}} --}}



{{!-- Link with stylesheet and script --}}
<script src="./js/profile.js"></script>
<link rel="stylesheet" href="/css/profile.css">




















{{!-- </div> --}}

{{!-- <button class="newPost-btn"></button> --}}



{{!-- <div class="row mt-4">
  <div class="col-md-6">
    <h3>What would you like to blog about?</h3>

    <form class="form mew-post-form">
      <div class="form-group">
        <label for="post-title">Post Title:</label>
        <input class="form-input" type="text" id="post-title" name="post-title" />
      </div>
      <div class="form-group">
        <label for="blog-post">Your Post:</label>
        <textarea class="form-input" id="blog-post" name="blog-post"></textarea>
      </div>
      <div class="form-group">
        <button type="submit" class="btn btn-primary">Post!</button>
      </div>
    </form>
  </div>
</div> --}}

    {{!-- compare logged_on.id to id in the post & only see logged on users posts --}}
    {{!-- If user.userName === blogPost.user_id --}}
    {{!-- {{#if (user.userName = blogPost.user_id)}} --}}
