<div class="text-center">
    <h2>{{name}}</h2>
    <p>{{description}}</p>
    <p>Created by {{user.name}} on {{format_date date_created}}</p>

    <p>
        <button class="btn btn-primary">I like 👍</button>
        <button class="btn btn-primary">Not a fan 👎</button>
    </p>
</div>

{{!-- !Link with css --}}