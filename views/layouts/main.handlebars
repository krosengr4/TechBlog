<!DOCTYPE html>
<html lang="en"> 

<head>
      <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Toaster Post</title>
  <link rel="stylesheet" href="/css/style.css">
</head>

<body class="background"> 
  <div class="flex-column justify-space-around">
    <header class="header">
      <h1>
        <span role="img" aria-label="money" class="main-icon">🍞</span>
        <a href="/" class="title-link">Toaster Post!</a>
      </h1>
      <nav>
        <!-- Conditionally render login or logout links -->
        {{!-- {{log this.logged_in}} --}}
        {{#if logged_in}}
        <a href="/profile" class="header-link"> Dashboard  </a>
        <a href="/" class="header-link">|||  Homepage  ||| </a> 
        <a href="/" class="header-link" id="logout"> Logout  </a>
        {{else}}
        <a href="/login" class="header-link">Login or Create User</button></a>
        {{/if}}
      </nav>
    </header>
    <main class="container container-fluid mt-5">
      <!-- Render the sub layout -->
      {{{ body }}}
    </main>
    <footer>
      
    </footer>
  </div>

  <!-- Render script for logged in users only -->
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>
</html> 


  {{!-- <link rel="stylesheet" href="/css/jass.css"> --}}